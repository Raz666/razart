import { Inter, Montserrat } from 'next/font/google';

export const mainFont = Inter({ subsets: ['latin'], display: 'swap' });
export const titleFont = Montserrat({ subsets: ['latin'], display: 'swap' });
